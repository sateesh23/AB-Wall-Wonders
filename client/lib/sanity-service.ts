// Sanity service placeholder - not currently implemented
export const sanityService = {
  isConnected: false,

  // Placeholder function for testing connection
  testConnection: async () => {
    return { success: false, message: "Sanity not configured" };
  },
};

export default sanityService;
